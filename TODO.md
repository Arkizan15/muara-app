# Flutter Muara App Fix & Development TODO

## Current Task: Fix Flutter Code
- [x] Create TODO.md
- [ ] Fix lib/pages/welcome.dart syntax and structure
- [ ] Run `flutter analyze` to validate lints/errors
- [ ] Run `flutter pub get`
- [ ] Test with `flutter run`

## Future Steps
- Add navigation from welcome screen (e.g., to login/home).
- Implement full app features using assets (logos, images).
- Add state management if needed.
- Run `flutter build` for release testing.
